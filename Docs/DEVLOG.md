# ECLIPSE FRONT — журнал разработки

Здесь фиксируются заметные изменения проекта, результаты проверок, известные ограничения и следующий практический шаг. Детальные требования остаются в GDD, архитектурных заметках, backlog и test plan соответствующего milestone.

## Правила ведения

- Новые записи добавляются сверху внутри даты.
- Отдельно указывается, что проверено сборкой, Automation, сетевым smoke test или вручную в PIE.
- Непройденная ручная проверка не помечается завершённой на основании одной успешной компиляции.
- Изменения gameplay-правил или сетевой модели также отражаются в GDD либо `TECHNICAL_ARCHITECTURE.md`.
- Временные логи Unreal хранятся в `Saved/Logs` и не заменяют эту запись.

## 2026-09-07

### M4.2 — Attack-move

- Верхняя `4` переводит локальный controller в режим выбора точки; следующий LMB отправляет серверу только destination.
- Сервер хранит attack-move, ищет ближайшую вражескую цель в радиусе 650 units и использует существующий `UEFCombatComponent` для атаки.
- После завершения боя герой продолжает исходный маршрут; RMB, прямая атака, `Space` и `H` отменяют приказ.
- Защищённые строения исключены из списка целей, а выбранная точка кратко отмечается голубым кругом.
- `EclipseFrontEditor Win64 Development` успешно собран; оба Automation-теста `EclipseFront.*` прошли, ручной сетевой тест ожидает выполнения.

## 2026-09-05

### M4 — Playable One-Lane Match

- Основной сценарий M3 принят владельцем проекта; дополнительные зеркальные проверки остаются записанными, но работа перешла к M4.
- Начат M4.1: добавлены серверные приказы Stop (`Space`) и Hold Position (`H`) с отменой движения, преследования, basic attack и непрерывной RMB-команды.
- Из-за конфликта с WASD управление камерой сохранено на `S`, Stop перенесён на `Space`, а центрирование камеры — с `Space` на `T`.
- Новая команда движения или атаки снимает Hold Position; сервер валидирует владельца, состояние героя и фазу матча.
- Добавлены `Docs/M4_BACKLOG.md` и `Docs/M4_TEST_PLAN.md`.
- `EclipseFrontEditor Win64 Development` успешно собран с изменениями M4.1.

### Репозиторий проекта

- Создан локальный Git-репозиторий с основной веткой `main`.
- Unreal-ассеты `.uasset` и `.umap` отмечены как бинарные и хранятся в обычном Git: текущий набор занимает около 33 МБ, а Git LFS аккаунта исчерпал доступный бюджет.
- Генерируемые каталоги Unreal и IDE исключены через `.gitignore`.
- Удалённый репозиторий: `https://github.com/Edziinshh/EclipseFront.git`.
- Добавлен `Docs/ROADMAP.md`: последовательность работ от закрытия M3 до основы 5v5 Alpha.

### M3 — Lane Structures и однолинейная осада

- Для завершения ручной проверки осады урон героев временно повышен до 5000: любое уязвимое строение, включая Nexus, разрушается одним попаданием; значения крипов и башен сохранены.
- Добавлены зеркальные цепочки строений `T1 → T2 → T3 → 2×T4 → Nexus`, а также melee/ranged barracks: всего 16 строений.
- Реализована серверная защита внутренних целей и последовательность открытия строений.
- Разрушение бараков усиливает соответствующий тип новых вражеских крипов.
- Победитель, усиления и завершение матча реплицируются через `AEFGameState`.
- После разрушения Nexus останавливаются волны, движение, атаки, снаряды и возрождения; HUD показывает результат.
- Башни используют реплицируемые снаряды с серверным попаданием и переключаются на героя, атаковавшего союзного героя под башней.
- По удержанию `Alt` локально отображается дальность атаки живых героев, крипов и башен.
- Крипы периодически пересматривают цель и переключаются с уведшего их героя на подошедшую вражескую волну.

### Диагностика

- Добавлены категории `LogEFCore`, `LogEFNetwork`, `LogEFMovement`, `LogEFCombat`, `LogEFAI` и `LogEFSiege` с указанием сетевого контекста.
- Добавлена интеграция с Visual Logger для команд движения, attack state, маршрутов крипов, aggro и башенной зоны.
- Правила использования логов описаны в `Docs/DEBUGGING.md`.

### Исправление маршрута линии

- Удалены три временных препятствия раннего M0, не относящиеся к дизайну линии.
- `NavMeshBoundsVolume` расширен по линии с `X=±6000` до `X=±8000` и сохранён как постоянно загруженный World Partition actor.
- Навигационные данные карты пересобраны для новых границ.
- Атака прототипной модели крипа стала заметнее: добавлены короткий выпад, усиленный импульс масштаба и цветовая вспышка.

### Проверено

- `EclipseFrontEditor Win64 Development`: сборка успешна.
- World Partition Navigation Data Builder: успешно обработаны четыре ячейки.
- `EclipseFront.Siege.ProtectionSequence`: Automation Success.
- `EclipseFront.Siege.ServerDamageAndUpgrades`: Automation Success.
- Независимая повторная загрузка карты подтвердила NavMesh bounds `X=±8000` и отсутствие старых препятствий.
- Ручной сетевой PIE-прогон (listen server + клиент): волны и обе стороны линии продолжили бой; последовательно разрушены Dusk T1, T2 и T3, затем обе Dusk-казармы; также крипами разрушена Dawn T1. Сервер открыл следующие цели в правильном порядке и зарегистрировал оба усиления крипов.
- В этом прогоне подтверждены last hit и награда 45 Gold, смерть/возрождение героя и отсутствие `Error`, `Ensure` и `Accessed None` в журнале.
- Повторный ручной PIE-прогон завершил полную осаду Dusk: обе T4 разрушены по очереди, Nexus открылся только после второй, затем победителем назначена Dawn и матч перешёл в `WaitingPostMatch`. Серверное окно показало `VICTORY`, клиентское — `DEFEAT`; после финала новые волны и боевые события не регистрировались.

### Остаётся проверить вручную

- Отдельно проверить отмену уже запланированного respawn после финала.
- Повторить полную осаду для Dawn, победив за Dusk, и проверить сброс состояния после перезапуска PIE.
- Завершить оставшиеся визуальные сценарии M3.2–M3.4 из `Docs/M3_SIEGE.md`.

### Следующий шаг

- Завершить ручную сетевую проверку M3, затем перейти к игровому представлению карты и следующему вертикальному срезу.

## 2026-09-04

### M0 — Project Foundation

- Создан Unreal Engine 5.8 C++ проект и gameplay module `EclipseFront`.
- Добавлены `AEFGameMode`, `AEFGameState`, `AEFPlayerState`, `AEFPlayerController`, герой и независимая MOBA-камера.
- Интегрирован GAS через проектный `UEFAbilitySystemComponent`; combat attributes отделены от Level/XP/Gold/KDA.
- Реализовано серверно проверяемое click-to-move управление и сетевой Test Match для двух игроков.

### M1 — Combat Loop

- Добавлены выбор цели и контекстный RMB attack order.
- Реализованы approach, attack point, backswing и basic attack damage через общий `CombatComponent + GAS` flow.
- Добавлены здоровье, смерть, возрождение и K/D; milestone пройден вручную в сетевом PIE.

### M2 — First Lane

- Добавлены серверные симметричные волны и движение крипов по заранее заданному NavMesh-маршруту.
- Реализованы creep aggro/combat, last hit, Gold, XP, уровни и временный HUD progression.
- Результаты ручных проверок зафиксированы в `Docs/M2_TEST_PLAN.md`.
